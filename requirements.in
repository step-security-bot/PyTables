# Runtime dependencies for ``pip install``.
# Keep list in sync with ``project.dependencies`` in ``pyproject.toml``.
# core dependencies
setuptools 
pip 
wheel 
build 
cython
# dependencies
numpy>=1.19.0
numexpr>=2.6.2
packaging
py-cpuinfo
blosc2>=2.2.8
# Sphinx and other dependencies
sphinx>=1.1,<6 
sphinx_rtd_theme 
numpydoc 
ipython
